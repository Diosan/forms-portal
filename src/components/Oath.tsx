import {useEffect, useState} from "react"
import { API_URL} from "../config/api"
import axios from "axios"

type OathProps = {}

export const Oath = ({}: OathProps) => {

    useEffect(() => {}, [])

    return (
        <>
            <h5>Complainant Oath</h5>
        </>
    )

}